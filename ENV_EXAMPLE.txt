# MongoDB Configuration
MONGODB_URI=mongodb+srv://saksham:LZ3L8RAYNiaysHvt@ecertificate.6lb9tpr.mongodb.net/?retryWrites=true&w=majority&appName=Ecertificate

# Backend SMTP configuration (Gmail example)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=your_gmail@gmail.com
SMTP_PASS=your_app_password
SMTP_FROM="Your Name <your_gmail@gmail.com>"

# Optional: enable test mode with Ethereal (no real emails)
# ETHEREAL=true

# Optional: server port (defaults to 3001)
PORT=3001

# Frontend config
VITE_EMAIL_SERVER_URL=http://localhost:3001
